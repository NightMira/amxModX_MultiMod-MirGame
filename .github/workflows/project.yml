name: Project Automation

on:
  issues:
    types: [opened, reopened, closed]
  pull_request:
    types: [opened, reopened, closed, ready_for_review]
  workflow_dispatch:

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  project-automation:
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 Checkout code (with private token)
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PRIVATE_REPO_TOKEN }}

      - name: 📋 Add new items to project
        if: github.event_name == 'issues' && github.event.action == 'opened' || github.event_name == 'pull_request' && github.event.action == 'opened'
        env:
          GH_TOKEN: ${{ secrets.PRIVATE_REPO_TOKEN }}
        run: |
          echo "🎯 Adding ${{ github.event_name }} to project..."
          
          if [ "${{ github.event_name }}" = "issues" ]; then
            ITEM_URL="https://github.com/NightMira/amxModX_MultiMod-MirGame/issues/${{ github.event.issue.number }}"
          else
            ITEM_URL="https://github.com/NightMira/amxModX_MultiMod-MirGame/pull/${{ github.event.pull_request.number }}"
          fi
          
          # Добавляем в проект организации MirGame-Studio
          gh project item-add 2 \
            --owner MirGame-Studio \
            --url "$ITEM_URL"
          
          echo "✅ Added to project successfully!"

      - name: 🏷️ Update project status
        if: github.event.action == 'closed' || github.event.action == 'reopened' || github.event.action == 'ready_for_review'
        env:
          GH_TOKEN: ${{ secrets.PRIVATE_REPO_TOKEN }}
        run: |
          echo "🔄 Updating project status for ${{ github.event_name }} #${{ github.event.issue.number || github.event.pull_request.number }}"
          
          # Получаем ID проекта
          PROJECT_ID=$(gh api graphql -f query='
            query {
              organization(login: "MirGame-Studio") {
                projectV2(number: 2) {
                  id
                }
              }
            }
          ' | jq -r '.data.organization.projectV2.id')
          
          if [ "${{ github.event_name }}" = "issues" ]; then
            CONTENT_ID="${{ github.event.issue.node_id }}"
          else
            CONTENT_ID="${{ github.event.pull_request.node_id }}"
          fi
          
          if [ -n "$PROJECT_ID" ] && [ -n "$CONTENT_ID" ]; then
            # Находим ID элемента в проекте
            ITEM_ID=$(gh api graphql -f query='
              query {
                node(id: "'$PROJECT_ID'") {
                  ... on ProjectV2 {
                    items(first: 50) {
                      nodes {
                        id
                        content {
                          ... on Issue {
                            id
                            number
                          }
                          ... on PullRequest {
                            id
                            number
                          }
                        }
                      }
                    }
                  }
                }
              }
            ' | jq -r '.data.node.items.nodes[] | select(.content.id == "'$CONTENT_ID'") | .id')
            
            if [ -n "$ITEM_ID" ]; then
              # Обновляем статус
              if [ "${{ github.event.action }}" = "closed" ]; then
                STATUS="Done"
              elif [ "${{ github.event.action }}" = "reopened" ] || [ "${{ github.event.action }}" = "ready_for_review" ]; then
                STATUS="In Progress"
              else
                STATUS="Todo"
              fi
              
              echo "📊 Setting status to: $STATUS"
              
              # Обновляем статус в проекте
              gh api graphql -f query='
                mutation {
                  updateProjectV2ItemFieldValue(input: {
                    projectId: "'$PROJECT_ID'",
                    itemId: "'$ITEM_ID'",
                    fieldId: "Status",
                    value: {
                      singleSelectOptionId: "'$STATUS'"
                    }
                  }) {
                    projectV2Item {
                      id
                    }
                  }
                }
              '
            fi
          fi
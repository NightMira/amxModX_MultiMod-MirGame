name: Project Automation

on:
  issues:
    types: [opened, reopened, closed]
  pull_request:
    types: [opened, reopened, closed, ready_for_review]
  workflow_dispatch:
    inputs:
      sync_all:
        description: 'Sync all open issues/PRs to project'
        required: false
        default: false
        type: boolean

jobs:
  project_automation:
    runs-on: ubuntu-latest
    
    steps:
      - name: Add new items to project
        uses: actions/add-to-project@v1.0.2
        with:
          project-url: https://github.com/users/NightMira/projects/3
          github-token: ${{ secrets.PRIVATE_REPO_TOKEN }}
        if: |
          github.event.action == 'opened' || 
          github.event.action == 'reopened' || 
          github.event.action == 'ready_for_review'

      - name: Update status for closed items
        if: github.event.action == 'closed'
        env:
          GH_TOKEN: ${{ secrets.PRIVATE_REPO_TOKEN }}
        run: |
          echo "🔄 Processing closed item..." >> $GITHUB_STEP_SUMMARY
          
          # Получаем данные проекта с правильными типами
          gh api graphql -f query='
            query($user: String!, $number: Int!) {
              user(login: $user) {
                projectV2(number: $number) {
                  id
                  fields(first:20) {
                    nodes {
                      ... on ProjectV2Field {
                        id
                        name
                        dataType
                      }
                      ... on ProjectV2SingleSelectField {
                        id
                        name
                        options {
                          id
                          name
                        }
                      }
                    }
                  }
                }
              }
            }' -f user="NightMira" -F number=3 > project_data.json
          
          PROJECT_ID=$(jq -r '.data.user.projectV2.id' project_data.json)
          STATUS_FIELD_ID=$(jq -r '.data.user.projectV2.fields.nodes[] | select(.name=="Status" and .dataType=="SINGLE_SELECT") | .id' project_data.json)
          DONE_OPTION_ID=$(jq -r '.data.user.projectV2.fields.nodes[] | select(.name=="Status" and .dataType=="SINGLE_SELECT") | .options[] | select(.name=="Done") | .id' project_data.json)
          
          if [ -n "$STATUS_FIELD_ID" ] && [ -n "$DONE_OPTION_ID" ]; then
            # Определяем content ID в зависимости от типа события
            if [ "${{ github.event_name }}" = "issues" ]; then
              CONTENT_ID="${{ github.event.issue.node_id }}"
            else
              CONTENT_ID="${{ github.event.pull_request.node_id }}"
            fi
            
            # Находим item в проекте
            ITEM_ID=$(gh api graphql -f query='
              query($project: ID!, $content: ID!) {
                node(id: $project) {
                  ... on ProjectV2 {
                    items(first: 100) {
                      nodes {
                        id
                        content {
                          ... on Issue {
                            id
                          }
                          ... on PullRequest {
                            id
                          }
                        }
                      }
                    }
                  }
                }
              }' -f project="$PROJECT_ID" -f content="$CONTENT_ID" \
              --jq ".data.node.items.nodes[] | select(.content.id == \"$CONTENT_ID\") | .id")
            
            if [ -n "$ITEM_ID" ]; then
              # Обновляем статус используя правильную мутацию
              gh api graphql -f query='
                mutation($project: ID!, $item: ID!, $field: ID!, $value: String!) {
                  updateProjectV2ItemFieldValue(input: {
                    projectId: $project
                    itemId: $item
                    fieldId: $field
                    value: {
                      singleSelectOptionId: $value
                    }
                  }) {
                    projectV2Item {
                      id
                    }
                  }
                }' -f project="$PROJECT_ID" -f item="$ITEM_ID" -f field="$STATUS_FIELD_ID" -f value="$DONE_OPTION_ID" --silent
              
              if [ $? -eq 0 ]; then
                echo "✅ Status updated to Done for item $ITEM_ID" >> $GITHUB_STEP_SUMMARY
              else
                echo "❌ Failed to update status" >> $GITHUB_STEP_SUMMARY
              fi
            else
              echo "⚠️ Item not found in project for content $CONTENT_ID" >> $GITHUB_STEP_SUMMARY
            fi
          else
            echo "⚠️ Status field or Done option not found" >> $GITHUB_STEP_SUMMARY
            echo "Status Field ID: $STATUS_FIELD_ID" >> $GITHUB_STEP_SUMMARY
            echo "Done Option ID: $DONE_OPTION_ID" >> $GITHUB_STEP_SUMMARY
          fi

      - name: Manual bulk sync
        if: github.event_name == 'workflow_dispatch' && github.event.inputs.sync_all == 'true'
        env:
          GH_TOKEN: ${{ secrets.PRIVATE_REPO_TOKEN }}
        run: |
          echo "### 🔍 Manual Bulk Sync" >> $GITHUB_STEP_SUMMARY
          echo "📦 Adding all open issues and PRs to project..." >> $GITHUB_STEP_SUMMARY
          
          # Получаем PROJECT_ID
          gh api graphql -f query='
            query($user: String!, $number: Int!) {
              user(login: $user) {
                projectV2(number: $number) {
                  id
                }
              }
            }' -f user="NightMira" -F number=3 > project_data.json
          
          PROJECT_ID=$(jq -r '.data.user.projectV2.id' project_data.json)
          
          # Функция для добавления в проект с правильными типами
          add_to_project() {
            local content_id="$1"
            local item_type="$2"
            local number="$3"
            local title="$4"
            
            # Используем правильную мутацию addProjectV2ItemById
            result=$(gh api graphql -f query='
              mutation($projectId: ID!, $contentId: ID!) {
                addProjectV2ItemById(input: {
                  projectId: $projectId,
                  contentId: $contentId
                }) {
                  projectV2Item {
                    id
                  }
                }
              }' -f projectId="$PROJECT_ID" -f contentId="$content_id" --silent 2>&1)
            
            if [ $? -eq 0 ]; then
              echo "✅ $item_type #$number: $title" >> $GITHUB_STEP_SUMMARY
              return 0
            else
              echo "⚠️ Failed to add $item_type #$number: $title" >> $GITHUB_STEP_SUMMARY
              echo "Error: $result" >> $GITHUB_STEP_SUMMARY
              return 1
            fi
          }
          
          # Получаем issues через GraphQL API с правильными типами
          echo "🔍 Processing issues..." >> $GITHUB_STEP_SUMMARY
          gh api graphql -f query='
            query($owner: String!, $repo: String!) {
              repository(owner: $owner, name: $repo) {
                issues(first: 100, states: OPEN, orderBy: {field: CREATED_AT, direction: DESC}) {
                  nodes {
                    number
                    id
                    title
                    state
                  }
                }
              }
            }' -f owner="${{ github.repository_owner }}" -f repo="${{ github.event.repository.name }}" > issues.json
          
          added_count=0
          while IFS= read -r issue; do
            if [ -n "$issue" ]; then
              number=$(echo "$issue" | jq -r '.number')
              id=$(echo "$issue" | jq -r '.id')
              title=$(echo "$issue" | jq -r '.title' | head -c 80)
              state=$(echo "$issue" | jq -r '.state')
              
              if [ "$state" = "OPEN" ]; then
                if add_to_project "$id" "Issue" "$number" "$title"; then
                  added_count=$((added_count + 1))
                fi
              fi
            fi
          done < <(jq -c '.data.repository.issues.nodes[]' issues.json)
          
          # Получаем PRs через GraphQL API с правильными типами
          echo "🔍 Processing PRs..." >> $GITHUB_STEP_SUMMARY
          gh api graphql -f query='
            query($owner: String!, $repo: String!) {
              repository(owner: $owner, name: $repo) {
                pullRequests(first: 100, states: OPEN, orderBy: {field: CREATED_AT, direction: DESC}) {
                  nodes {
                    number
                    id
                    title
                    state
                  }
                }
              }
            }' -f owner="${{ github.repository_owner }}" -f repo="${{ github.event.repository.name }}" > prs.json
          
          while IFS= read -r pr; do
            if [ -n "$pr" ]; then
              number=$(echo "$pr" | jq -r '.number')
              id=$(echo "$pr" | jq -r '.id')
              title=$(echo "$pr" | jq -r '.title' | head -c 80)
              state=$(echo "$pr" | jq -r '.state')
              
              if [ "$state" = "OPEN" ]; then
                if add_to_project "$id" "PR" "$number" "$title"; then
                  added_count=$((added_count + 1))
                fi
              fi
            fi
          done < <(jq -c '.data.repository.pullRequests.nodes[]' prs.json)
          
          echo "🎯 Manual sync completed! Added $added_count items to project." >> $GITHUB_STEP_SUMMARY
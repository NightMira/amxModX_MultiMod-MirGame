name: Project Automation

on:
  issues:
    types: [opened, reopened, closed]
  pull_request:
    types: [opened, reopened, closed, ready_for_review]
  workflow_dispatch:

permissions:
  contents: read
  issues: write
  pull-requests: write

jobs:
  project-automation:
    runs-on: ubuntu-latest
    steps:
      - name: 🚀 Checkout code (with private token)
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PRIVATE_REPO_TOKEN }}

      - name: 📋 Add new items to project (Simple version)
        if: github.event_name == 'issues' && github.event.action == 'opened' || github.event_name == 'pull_request' && github.event.action == 'opened'
        env:
          GH_TOKEN: ${{ secrets.PRIVATE_REPO_TOKEN }}
        run: |
          echo "🎯 Adding ${{ github.event_name }} to project..."
          
          if [ "${{ github.event_name }}" = "issues" ]; then
            ITEM_URL="https://github.com/NightMira/amxModX_MultiMod-MirGame/issues/${{ github.event.issue.number }}"
            echo "📝 Issue URL: $ITEM_URL"
          else
            ITEM_URL="https://github.com/NightMira/amxModX_MultiMod-MirGame/pull/${{ github.event.pull_request.number }}"
            echo "📝 PR URL: $ITEM_URL"
          fi
          
          # Простая команда добавления без GraphQL
          echo "🏗️ Adding to project #3..."
          if gh project item-add 3 --owner NightMira --url "$ITEM_URL"; then
            echo "✅ Added to project successfully!"
          else
            echo "⚠️ Failed to add to project. Checking token permissions..."
            
            # Проверяем права токена
            echo "🔍 Testing token permissions for projects..."
            if gh project view 3 --owner NightMira >/dev/null 2>&1; then
              echo "✅ Token can view project"
            else
              echo "❌ Token cannot access project"
              echo "ℹ️ Please ensure PAT has 'project' permissions"
            fi
          fi

      - name: 📋 Alternative project addition via API
        if: always() && (github.event_name == 'issues' && github.event.action == 'opened' || github.event_name == 'pull_request' && github.event.action == 'opened')
        env:
          GH_TOKEN: ${{ secrets.PRIVATE_REPO_TOKEN }}
        run: |
          echo "🔄 Trying alternative method..."
          
          # Альтернативный метод через REST API
          if [ "${{ github.event_name }}" = "issues" ]; then
            CONTENT_ID="${{ github.event.issue.node_id }}"
          else
            CONTENT_ID="${{ github.event.pull_request.node_id }}"
          fi
          
          # Пытаемся добавить через API
          response=$(curl -s -w "%{http_code}" \
            -X POST \
            -H "Authorization: token $GH_TOKEN" \
            -H "Accept: application/vnd.github+json" \
            -H "X-GitHub-Api-Version: 2022-11-28" \
            "https://api.github.com/projects/columns/cards" \
            -d '{
              "content_id": "'$CONTENT_ID'",
              "content_type": "'${{ github.event_name }}'"
            }' 2>/dev/null)
          
          http_code=$(echo "$response" | tail -n1)
          if [ "$http_code" = "201" ]; then
            echo "✅ Added to project via API"
          else
            echo "❌ API call failed with HTTP $http_code"
            echo "⚠️ Project automation requires additional permissions"
          fi